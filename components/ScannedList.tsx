import { StyleSheet, Text, View } from 'react-native'

export default function ScannedList() {
  return (
    <View style={{marginTop: 30}}>
      <Text>no locations visited..</Text>
    </View>
  )
}

const styles = StyleSheet.create({})